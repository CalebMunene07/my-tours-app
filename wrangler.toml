# wrangler.toml — place in your Next.js project root (same folder as package.json)
# Cloudflare Pages requires "pages_build_output_dir" not "publish"

name = "wikima"
compatibility_date = "2024-01-01"
pages_build_output_dir = "out"
